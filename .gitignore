# dependencies
/node_modules
/.pnp
.pnp.js

# Next.js 本地构建产物（本地npm run build输出，云端Pages会重新构建，不要提交）
/.next
/out

# @cloudflare/next‑on‑pages 本地转换产物 ⭐【重点，禁止提交！】
# 你WSL本地执行 npx @cloudflare/next-on-pages 生成的 .vercel，不要上传git
/.vercel

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files 环境变量，本地密钥不要提交
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# wrangler 本地缓存（就算next项目里面装了wrangler dev依赖，缓存不要提交）
.wrangler